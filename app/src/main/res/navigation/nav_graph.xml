<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/songListFragment">

    <fragment
        android:id="@+id/songDetailFragment"
        android:name="com.pskda.androidlab.fragment.SongFragment"
        android:label="TrackDetailFragment"
        tools:layout="@layout/fragment_song_one">
        <action
            android:id="@+id/action_songDetailFragment_to_songListFragment"
            app:destination="@id/songListFragment" />
    </fragment>
    <fragment
        android:id="@+id/songListFragment"
        android:name="com.pskda.androidlab.fragment.SongListFragment"
        android:label="TrackListFragment"
        tools:layout="@layout/fragment_song_list">
        <action
            android:id="@+id/action_songListFragment_to_songDetailFragment"
            app:destination="@id/songDetailFragment" />
    </fragment>

</navigation>